{
  "name": "-markup-with-vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:sass": "sass --no-source-map src/scss/:dist/css/",
    "build": "vite build && npm run build:sass",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sass": "^1.75.0",
    "scss": "^0.2.4",
    "vite": "^5.2.0",
    "vite-plugin-handlebars": "^2.0.0"
  }
}
